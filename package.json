{
  "name": "mygh",
  "version": "0.1.0",
  "private": true,
  "description": "Better social preview links for GitHub repositories and releases.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.0.0"
  }
}
